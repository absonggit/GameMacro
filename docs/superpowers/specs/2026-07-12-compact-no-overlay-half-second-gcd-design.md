# 紧凑主界面与 0.5 秒公共 CD 设计

程序保持纯视觉选择，不计算技能本地 CD。成功发送任意技能按键后锁定全部发送 0.5 秒，随后重新扫描。技能仍使用单技能就绪连续帧防抖。

完全移除悬浮窗及其最小化、置顶、穿透和位置持久化运行逻辑。主窗口保持可见。

技能队列外框固定为两排八列卡片与加删按钮所需宽度，不再横向填满主窗口。技能设置压缩为一行：按键下拉、框选捕获、捕获状态、应用。控制热键下拉移到底部状态栏，与状态、启动和紧急停止按钮同行。主窗口默认高度缩小。

测试覆盖 0.5 秒边界；Release 构建和启动检查验证 XAML 与删除悬浮窗后的生命周期。
